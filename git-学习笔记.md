# Git 学习笔记

> 这份笔记整理了我在 Trae 云端里和 AI 助手讨论 Git 时学到的概念。
> 适用于完全不懂代码的小白,所有概念都用大白话+类比讲解。

***

## 一、先分清两个东西

很多人搞混 Git 和 GitHub,其实它们是两个完全不同的东西:

| 名字         | 是什么                | 要不要下载         |
| ---------- | ------------------ | ------------- |
| **Git**    | 一个记录代码改动的工具(命令行工具) | 不用,云端服务器已经装好了 |
| **GitHub** | 放代码的网址,你的网页就存在这    | 不用下载,网页登录即可   |

**类比理解**:

* Git = 后厨厨师(干活的人)

* GitHub = 外卖平台(展示和配送的网站)

* Trae = 餐厅服务员(你点菜,他转达给后厨)

你点菜(告诉 AI 改什么)→ 后厨做菜(Git 记录改动)→ 外卖平台送餐(GitHub 更新网页)。

***

## 二、Git 是什么?一句话解释

Git 就像一个 **"时光机" + "备份箱"**:

* 你改网页代码的时候,Git 可以帮你记住每一次改动

* 改错了可以回到之前的样子

* 改好了可以上传到 GitHub,让网页也跟着更新

***

## 三、Git 在哪里?需要下载吗?

**不用下载,已经有了。**

Git 不在桌面上,也不在 Trae 的某个按钮里。它藏在 **运行这个对话的云端服务器** 里。

就像手机出厂就带了计算器,这个云端服务器出厂就带了 Git。

验证方式:执行 `git version`,会回复类似 `git version 2.43.0`,说明已装好。

**关键**:Git 不是微信那种要登录的 App,它是个命令行工具,通过命令调用。

***

## 四、改网页需要哪几步?(基础工作流)

就 4 步,反复循环:

| 步骤 | 大白话        | 对应命令                     |
| -- | ---------- | ------------------------ |
| 1  | 下载代码到本地    | `git clone`              |
| 2  | 在本地改文件     | 用编辑器/Trae 改              |
| 3  | 保存改动记录     | `git add` + `git commit` |
| 4  | 上传到 GitHub | `git push`               |

### 怎么才算"用了 Git"?

Git 就像记账本。你在记账本上写一笔,才算用了它。最基础的三笔账:

1. **`git add 文件名`** —— 我要把这几个改过的文件记下来
2. **`git commit -m "说明文字"`** —— 这次改动正式存档,写上备注
3. **`git push`** —— 把存档上传到 GitHub,网页跟着更新

只要执行了上面任意一步,就是"用了 Git"。

***

## 五、查看 Git 提交记录

用这个命令查看所有历史版本:

```bash
git log --oneline --all --graph -10
```

输出长这样:

```
* eda6d4d Add files via upload
* a07d443 init: 梨的工作台
```

* 最上面一行是**最新版本**

* `eda6d4d` 这种是版本代号(提交哈希)

* 后面是提交说明

***

## 六、Git 记录的是什么?不是什么?

### ✅ Git 记录的:

* **代码被改动的历史**(每一次 add + commit)

### ❌ Git 不记录的:

* **聊天对话内容**

* **没提交的临时改动**

**重要理解**:Git 记录是 **属于项目的**,不是属于某个对话的。

不管你在哪个对话里操作,只要改的是同一个项目仓库,提交记录都是连续的、共享的。

如果某个对话改了代码但**没上传到 GitHub**,那这些改动就只存在那个对话的云端环境里,新对话拿不到。

***

## 七、回退(后悔药)怎么用?

回退分两种情况:

| 情况                 | 怎么办                    |
| ------------------ | ---------------------- |
| **还没保存提交**(只是改了一半) | 直接说"撤销当前改动",恢复到改之前     |
| **已经保存提交了**        | 查历史记录,列出几个版本给你选,你挑一个回退 |

### 命令版本

```bash
# 切回某个版本(看但不改)
git checkout <版本代号>

# 撤销当前工作区改动
git restore <文件名>

# 删除最近一次提交(保留改动)
git reset --soft HEAD~1
```

**小白建议**:不用自己记命令,直接跟 AI 说"回到 xxx 版本"即可。

***

## 八、分支是什么?

主分支(`main`)就像你家**客厅**,稳定版本住在这里。

新分支就像你搭了个**临时棚子**,在里面随便改:

* 改坏了,棚子不要了,客厅还在

* 改好了,把棚子里的东西搬进客厅(合并)

### 常用分支命令

```bash
# 开一个新分支并切过去
git checkout -b 分支名

# 查看所有分支(星号表示当前所在)
git branch

# 切回主分支
git checkout main

# 删除分支(必须先切走)
git branch -d 分支名

# 把分支合并到当前分支
git merge 分支名
```

***

## 九、分支名只是标签,不是功能!

**这是最容易误会的地方,重点理解**:

开一个叫 `feature-暗色模式` 的分支,**不会自动让这个分支具备暗色模式功能**。
功能是靠 **在里面写代码** 实现的。

### 类比

分支就像一个**空文件夹**,名字是你贴在文件夹上的标签:

* 你贴个标签叫"暗色模式"

* 但文件夹里一开始是空的(其实是 main 的复制版)

* 要往里面塞暗色模式的代码,它才真的有这个功能

如果只开分支不写代码,那它和 main 一模一样,只是名字不同。

### 约定俗成的命名规范(只是习惯,不是规则)

| 前缀          | 含义    | 例子              |
| ----------- | ----- | --------------- |
| `feat-`     | 新功能   | `feat-暗色模式`     |
| `fix-`      | 修 bug | `fix-按钮错位`      |
| `style-`    | 改样式   | `style-优化配色`    |
| `refactor-` | 重构代码  | `refactor-拆分模块` |
| `docs-`     | 改文档   | `docs-更新说明`     |
| `chore-`    | 杂务    | `chore-更新依赖`    |

***

## 十、Commit Message(提交信息)怎么写?

强迫你写下简短的备注,两周后回看就是最好的文档。

### 推荐格式

```
类型: 简短说明
```

**例子**:

* `feat: 增加暗色模式快捷切换`

* `fix: 修复窗口缩放时按钮错位`

* `style: 调整主色调为暖色`

* `refactor: 拆分状态管理模块`

***

## 十一、上传到 GitHub 需要认证(钥匙)

GitHub 就像一个带锁的仓库,上传需要一把"钥匙"。这把钥匙可以是:

1. **GitHub 账号密码**(现在不太推荐)
2. **Personal Access Token** ⭐ 推荐,一串字母数字
3. **SSH 密钥**(一对文件)

### 手机端也能完成

* 在手机上生成 Token,复制粘贴给 AI

* 或在手机上登录 GitHub 网页手动上传

* 不一定需要电脑

***

## 十二、Git 的三大核心价值

### 1. 做"后悔药"的精准锚点

优化某个配置或重构某段代码后,发现效果还不如之前。用 Git,你可以瞬间(秒级)回到昨天甚至上周的任意版本,不用靠 Ctrl+Z 瞎按,也不用手动备份一堆 `config_backup_v3_final_realyong.zip`。

### 2. 支持"胡思乱想"的实验性开发

你对工作台有个大胆的新布局想法,但又舍不得现在的稳定版。用 Git 分支,你可以 1 秒钟创建一个叫 `experiment-new-layout` 的分支,放开手脚去改:

* 改完觉得不行 → 切回主分支,删除实验分支,干干净净

* 改完觉得好 → 合并回来即可

### 3. 清晰地记录"为什么这么改"

个人项目最容易犯的毛病是"当时瞎改的,现在忘了为什么"。Git 的提交信息强迫你写下简短的备注。两周后回头看你就是自己最好的文档。

***

## 十三、三种优化工作台的方式对比

| 方式          | 是什么                   | Token 消耗                |
| ----------- | --------------------- | ----------------------- |
| **在分支里改**   | Git 的版本管理手段,在同一对话里搞实验 | 几乎不增加(几条 git 命令)        |
| **新开对话**    | 完全重启一个聊天,从头开始         | 最费(要重新读代码、重新建立上下文)      |
| **子 agent** | 在当前对话里派一个"分身"去干独立的活   | 中等(子 agent 自己消耗一份,主对话省) |

### 关键区别

它们**解决不同问题,不是替代关系**:

* **分支** 解决:改坏了能回退

* **新对话** 解决:上下文太乱想重开

* **子 agent** 解决:有个独立任务不想污染主对话

可以组合用,比如:在分支里 + 用子 agent + 新对话,都行。

### 为什么"在分支里改"最省 Token?

* 分支是 Git 的版本标签,跟代码内容无关

* 分支里存的代码,内容和 main **完全一样**(只是从 main 复制了一份)

* 之前在 main 上读过的代码,在分支里还是那一份,**不用重读**

* 所以开分支**不会**让我多读代码,不费 Token

### 真正消耗 Token 的是什么?

| 动作          | 是否消耗 Token  |
| ----------- | ----------- |
| 开分支、切分支、删分支 | 几乎不消耗(几行命令) |
| 读代码文件       | 消耗(文件越大越费)  |
| 改代码         | 消耗(输出改动内容)  |
| 讨论方案        | 消耗(对话本身)    |

***

## 十四、给小白的实操建议

### 1. 分支策略

不用纠结分支策略:

* **小改动**(改个颜色、调个间距)→ 直接在 main 上改,不开分支

* **大改动或实验性**(加新功能、试新布局)→ 开分支,好就合并,不好就删

### 2. Token 优化建议

1. **就用当前这个对话**,最省(不要轻易新开)
2. **该开分支时开分支**(实验性大改动)
3. **新对话留到上下文太乱时再用**
4. **子 agent 暂时不用**,任务简单时反而绕弯路

### 3. 实操节奏

1. 每次优化前,先开一个分支(实验性改动)
2. 改完一小批,提交一次,写清楚备注
3. 改完测一下,好的话合并回主分支
4. 不好的话直接删分支,主分支毫发无损
5. 最后统一上传到 GitHub

***

## 十五、我的项目现状参考

(以 `jkoyguj/li-workbench` 为例)

* **仓库地址**:`https://github.com/jkoyguj/li-workbench`

* **网页地址**:`https://jkoyguj.github.io/li-workbench/personal-workbench.html`

* **本地路径**:`/workspace/li-workbench`

* **默认分支**:`main`(GitHub Pages 默认分支)

* **初始提交记录**:

  * `a07d443` init: 梨的工作台

  * `eda6d4d` Add files via upload

***

## 速查命令表

| 我想要...       | 命令                      |
| ------------ | ----------------------- |
| 看版本号         | `git version`           |
| 看改动状态        | `git status`            |
| 看具体改动        | `git diff`              |
| 看历史记录        | `git log --oneline -10` |
| 暂存改动         | `git add 文件名`           |
| 提交存档         | `git commit -m "说明"`    |
| 上传到 GitHub   | `git push origin main`  |
| 拉取 GitHub 最新 | `git pull origin main`  |
| 开新分支         | `git checkout -b 分支名`   |
| 切换分支         | `git checkout 分支名`      |
| 看所有分支        | `git branch`            |
| 删除分支         | `git branch -d 分支名`     |
| 合并分支         | `git merge 分支名`         |

***

## 最后的话

> 你不需要记住所有命令。
> 你只需要知道:**想做什么,跟 AI 说一声就行**。
> AI 会调用 Git 帮你执行,你只管看结果、做决定。
>
> Git 的价值不在命令,在于养成"小步提交、写清备注、敢做实验"的习惯。
> 习惯有了,工具自然就熟了。

